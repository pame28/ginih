DECLARE @FECHATOKEN VARCHAR(20) = '2023-06';

UPDATE WS_CobranzasON
SET Dominio =
    LEFT(Dominio,
         LEN(Dominio)
         - CHARINDEX('|', REVERSE(Dominio))
    )
    + '|' + @FECHATOKEN
WHERE Nombre_Cliente = 'COBR_GINIH';
